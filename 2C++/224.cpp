/** 1. 思路
 * 先前缀表达式转后缀表达式
 * 再后缀表达式求值
 */
#include <string>
#include <stack>
#include <vector>
using namespace std;
class Solution {
public:
    vector<string> ReversePolish;

    void toReversePolish(string s)
    {
        for(int i = 0; i < s.size(); i++)
        {
            if()
        }
    }

    int calculate(string s) {
        // 先转化为逆波兰表达式, 再用逆波兰表达式求值
        
    }
};